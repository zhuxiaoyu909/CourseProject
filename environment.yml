# Create environment:
# conda env create -f environment.yml
#
# Update environment when there is an change:
# conda env update -f environment.yml --prune
#
# Remove environment:
# conda remove --name cs410-project --all
#
# Frequently used command:
# conda env list
# conda activate [env name: cs410-project]
# pip list
#
name: cs410-project
channels:
  - defaults
dependencies:
  - pip=21.0.1
  - python=3.6
  - Cython
  - pip:
      - --requirement requirements.txt